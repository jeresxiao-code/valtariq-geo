import { createClient } from "@/lib/supabase/server";export async function getAudits(projectId:string){const s=await createClient();const {data,error}=await s.from("geo_audits").select("id,url,status,score,summary,created_at,completed_at,geo_audit_issues(id,category,severity,title,detail,action)").eq("project_id",projectId).order("created_at",{ascending:false});if(error)throw error;return data??[];}