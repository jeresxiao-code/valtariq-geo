import type{AuditFinding}from"@/lib/audit/types";
export interface RecommendationInput{auditFindings:AuditFinding[];citationRate:number;mentionRate:number;ownedCitationCount:number;competitorSov:number}
export interface GeneratedRecommendation{source:string;sourceKey:string;title:string;description:string;action:string;priority:"critical"|"high"|"medium"|"low";impactScore:number;effortScore:number}
export function generateRecommendations(i:RecommendationInput):GeneratedRecommendation[]{const out:GeneratedRecommendation[]=[];for(const f of i.auditFindings.filter(x=>x.status!=="pass"))out.push({source:"audit",sourceKey:f.key,title:`Fix: ${f.label}`,description:f.evidence,action:f.recommendation??"Resolve this audit issue.",priority:f.maxScore>=12?"high":"medium",impactScore:f.maxScore,effortScore:f.key.includes("schema")?5:3});if(i.mentionRate<50)out.push({source:"visibility",sourceKey:"low-mention-rate",title:"Increase AI brand mention coverage",description:`Brand mention rate is ${i.mentionRate}%.`,action:"Expand buyer-question content around products, applications, comparisons and supplier-selection queries.",priority:"high",impactScore:18,effortScore:7});if(i.citationRate<35||i.ownedCitationCount===0)out.push({source:"citation",sourceKey:"citation-gap",title:"Build stronger owned citation targets",description:"AI answers rarely cite the project's own domain.",action:"Create evidence-rich product, technical, FAQ and case-study pages with clear factual claims and stable URLs.",priority:"high",impactScore:20,effortScore:7});if(i.competitorSov>50)out.push({source:"competitor",sourceKey:"sov-gap",title:"Close competitor share-of-voice gap",description:`Tracked competitors hold approximately ${i.competitorSov}% of measured share of voice.`,action:"Prioritize prompts where competitors appear and publish direct comparison, alternative and category-authority content.",priority:"high",impactScore:17,effortScore:6});return out.sort((a,b)=>(b.impactScore-b.effortScore)-(a.impactScore-a.effortScore)).slice(0,12)}
