import{NextResponse}from"next/server";export async function GET(req:Request){if(req.headers.get("authorization")!==`Bearer ${process.env.WORKER_SECRET}`)return NextResponse.json({error:"unauthorized"},{status:401});const p={chatgpt:!!process.env.OPENAI_API_KEY,claude:!!process.env.ANTHROPIC_API_KEY,gemini:!!process.env.GEMINI_API_KEY,perplexity:!!process.env.PERPLEXITY_API_KEY,deepseek:!!process.env.DEEPSEEK_API_KEY};return NextResponse.json({ok:Object.values(p).every(Boolean),providers:p,time:new Date().toISOString()});}