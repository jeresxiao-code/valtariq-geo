import Link from "next/link";import { PageShell } from "@/components/page-shell";import { getProjects } from "@/lib/data/projects";export default async function ProjectsPage(){const ps=await getProjects();return <PageShell title="Projects" description="Manage brands and GEO monitoring workspaces."><div className="grid grid-cols-2 gap-4">{ps.map((p:any)=><Link key={p.id} href={"/?project="+p.id} className="rounded-lg border border-border bg-panel p-6 hover:border-accent"><div className="font-medium">{p.brand_name}</div><div className="mt-2 text-sm text-muted">{p.domain}</div><div className="mt-4 text-xs text-muted">{p.name}</div></Link>)}</div><Link href="/onboarding" className="mt-6 inline-block rounded-md bg-accent px-4 py-2 text-sm font-medium text-black">New project</Link></PageShell>;}