import { PageShell } from "@/components/page-shell";import { getRunResult } from "@/lib/data/run-results";export default async function RunDetail({params}:{params:Promise<{id:string}>}){const {id}=await params,r:any=await getRunResult(id);return <PageShell title={r.provider+" Result"} description={r.prompts?.prompt??"AI response"}><div className="grid grid-cols-[1.5fr_1fr] gap-6"><div className="rounded-lg border border-border bg-panel p-6"><h2 className="font-medium">AI response</h2><pre className="mt-4 whitespace-pre-wrap font-sans text-sm">{r.response_text}</pre></div><div className="space-y-6"><div className="rounded-lg border border-border bg-panel p-5">Brand mentioned: {r.brand_mentioned?"Yes":"No"}<div className="text-sm text-muted">Position: {r.brand_position??"—"}</div></div><div className="rounded-lg border border-border bg-panel p-5"><h2>Citations</h2>{(r.citations??[]).map((c:any)=><div key={c.id} className="mt-3 text-sm">{c.domain}<div className="truncate text-xs text-muted">{c.url}</div></div>)}</div></div></div></PageShell>;}