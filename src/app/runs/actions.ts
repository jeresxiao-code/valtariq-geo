"use server";import{revalidatePath}from"next/cache";import{createClient}from"@/lib/supabase/server";import{requireRunCapacity}from"@/lib/billing/enforce";import type{AIProviderId}from"@/lib/providers/types";const providers:AIProviderId[]=["chatgpt","gemini","claude","perplexity","deepseek"];export async function queuePromptRun(fd:FormData){const s=await createClient(),projectId=String(fd.get("project_id")??""),promptId=String(fd.get("prompt_id")??"");if(!projectId||!promptId)return;await requireRunCapacity(providers.length);const{error}=await s.from("run_jobs").insert(providers.map(provider=>({project_id:projectId,prompt_id:promptId,provider,status:"queued"})));if(error)throw error;revalidatePath("/prompts");}