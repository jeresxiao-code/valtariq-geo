export function TrendBars({data}:{data:{date:string;visibility:number;citations:number}[]}){if(!data.length)return <div className="text-sm text-muted">Run monitoring over time to build a visibility trend.</div>;return <div className="flex h-48 items-end gap-2">{data.map(d=><div key={d.date} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t bg-accent" style={{height:Math.max(4,d.visibility*1.5)}} title={d.visibility+"% visibility"}/><span className="text-[10px] text-muted">{d.date.slice(5)}</span></div>)}</div>}